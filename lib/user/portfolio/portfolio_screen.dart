import 'package:flutter/material.dart';

class UserPortfolioScreen extends StatelessWidget {
  const UserPortfolioScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('User Portfolio'),
      ),
    );
  }
}
