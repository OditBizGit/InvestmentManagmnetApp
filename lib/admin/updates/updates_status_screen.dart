import 'package:flutter/material.dart';

class AdminUpdatesStatusScreen extends StatelessWidget {
  const AdminUpdatesStatusScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Updates / Status'),
    );
  }
}
