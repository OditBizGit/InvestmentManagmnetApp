import 'package:flutter/material.dart';

class AdminWorkProgressScreen extends StatelessWidget {
  const AdminWorkProgressScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Work Progress'),
    );
  }
}
