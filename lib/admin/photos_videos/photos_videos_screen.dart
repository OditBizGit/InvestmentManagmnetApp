import 'package:flutter/material.dart';

class AdminPhotosVideosScreen extends StatelessWidget {
  const AdminPhotosVideosScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Photos & Videos'),
    );
  }
}
