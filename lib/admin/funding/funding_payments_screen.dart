import 'package:flutter/material.dart';

class AdminFundingPaymentsScreen extends StatelessWidget {
  const AdminFundingPaymentsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Funding & Payments'),
    );
  }
}
